# APIx prototype (SIH26056) — Python dependencies
#
# NOTE: sqlite3 is part of the Python standard library and is deliberately NOT
# listed here. The `sqlite3` package on PyPI is an unrelated third-party project.

# --- API layer ---
fastapi
uvicorn[standard]

# --- Data / index math ---
pandas
numpy

# --- Config ---
pyyaml

# --- Scraper (Phase 3). Browser binaries are installed separately with:
#     python -m playwright install chromium
playwright

# --- Dashboard (Phase 8) ---
streamlit
plotly

# --- Testing ---
pytest
httpx          # required by fastapi.testclient
